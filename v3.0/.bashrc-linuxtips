# ---------------------------------------------------------

alias tip="sh ~/scripts/linuxtips.sh"
# Set TOTD to HTTP mode (alternative is local files mode) -----
export TOTD_HTTP=1
sh ~/scripts/linuxtips.sh

